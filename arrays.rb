# Create a new array of strings with the %w special syntax.
a = %w[pending resolved array rejected]
p a

# Create a new array of symbols with the %i special syntax
b = %i[foo bar pending resolved array rejected]
p b